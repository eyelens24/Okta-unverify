Put the jumpscare image here as:  jumpscare.png

The chaos layer (js/chaos.js) shows public/img/jumpscare.png when the player
misses the tiny X on an ad. Until you drop that file in, a big emoji fallback
is shown instead, so everything still works.

Save the scary rat image you want as:  public/img/jumpscare.png
(any web image format works if you also update the path in js/chaos.js.)
